# Admin application testing

Run from the repository root:

```console
npm test --workspace @mcc/admin
npm run app:synth -- admin
```

Add API contract tests and UI component/end-to-end tests as those surfaces are
implemented.
