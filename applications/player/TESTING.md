# Player application testing

Run from the repository root:

```console
npm test --workspace @mcc/player
npm run app:synth -- player
```

Add API contract and UI tests as the gold-layer read model is implemented.
